# Audit 03-21-2026

## Suggested Fix Implementation Plan

### Active To Dos

1. [x] Save this audit and convert the plan into actionable to dos.
2. [x] Critical: preserve 3D coordinates and define AS edge export behavior in core `build_etv_dataset`.
3. [x] Critical: unify the single-MF GUI handoff with the AS distance-pipeline path.
4. [x] Critical: use the full dataset label set for dataset-to-AS ingestion and warn on dropped edges.
5. [x] Add and update tests for the critical fixes.
6. [x] High: resolve `OptimalChoice` Procrustes contract drift.
7. [x] High: replace zero-filled distance-mode centrality with a clearer contract.
8. [x] High: separate generic distance-matrix naming from structural-equivalence naming.
9. [x] High: update README and other docs to match the real MF -> AS bridge.
10. [x] High: expand integration coverage for temporal alignment quality and bridge consistency.
11. [x] Medium: align GUI MF defaults with library defaults.
12. [x] Medium: expose or document hidden MF GUI options.
13. [x] Medium: align file picker affordances with actual ingestion behavior.
14. [x] Medium: decide whether AS JSON should include `procrustes` and `etv_dataset`.
15. [x] Medium: reconcile weighted vs unweighted centrality methodology.
16. [x] Low: reconcile `MdsDimMode::Visual` naming across code and docs.
17. [x] Low: remove MF's dependency on AS-owned `SimToDistMethod`.

## Severity / Difficulty Table

| Severity | Difficulty | Description |
|---|---|---|
| Critical | Medium | Fix `crates/as-pipeline/src/pipeline.rs` `build_etv_dataset` so it preserves `z` coordinates for 3D outputs and exports adjacency-backed edges with a clear undirected policy. |
| Critical | Medium | Refactor `crates/lv-gui/src/as_panel.rs` so single MatrixForge output uses the same AS distance-pipeline flow as MatrixForge series. |
| Critical | Medium | Rebuild dataset-to-AS label handling in `crates/lv-gui/src/as_panel.rs` using the full dataset label union and warn when edges reference unknown labels. |
| High | Medium | Implement real `OptimalChoice` Procrustes or rename/remove it so the API matches behavior. |
| High | Medium | Replace zero-filled centrality in `run_distance_pipeline` with propagated MF centrality, explicit absence, or a documented distance-based alternative. |
| High | Medium | Split generic distance matrix semantics from structural-equivalence semantics in `crates/as-pipeline/src/types.rs`. |
| High | Low | Update `README.md` and related docs to state that MF produces similarity matrices that AS converts to distance. |
| High | Medium | Add integration tests for label stability, temporal alignment quality, 3D export fidelity, and single-MF vs series bridge consistency. |
| Medium | Low | Align GUI defaults in `crates/lv-gui/src/mf_panel.rs` with `MfConfig::default()`. |
| Medium | Low | Expose or document hidden GUI-controlled MF settings such as `unicode_normalize`, `shared_vocabulary`, and `min_tokens_per_slice`. |
| Medium | Low | Align directory/file picker behavior with real ingestion support or expand ingestion support beyond `.txt`. |
| Medium | Low | Decide whether AS JSON should remain a partial analytical artifact or include `procrustes` and `etv_dataset`. |
| Medium | Medium | Reconcile MF and AS centrality methodology, especially weighted vs unweighted betweenness. |
| Low | Low | Reconcile the meaning of `MdsDimMode::Visual` across code, UI, and planning docs. |
| Low | Medium | Move `SimToDistMethod` to a shared or MF-owned location to reduce cross-crate coupling. |

## Notes on the Critical Fixes

- Core AS export should now preserve actual 3D coordinates and carry adjacency-backed edges into LV-ready sheets.
- Single-step MatrixForge embedding should no longer bypass the AS distance pipeline.
- Dataset ingestion for AS should no longer assume the first sheet defines the full label universe.

## Notes on the High Fixes

- `OptimalChoice` now evaluates every timestep as a candidate anchor, aligns every other timestep against that candidate with Procrustes, sums the residuals, and chooses the lowest-total-residual reference. This replaces the old implicit step-0 fallback.
- Distance-pipeline centrality is now explicit: precomputed-distance runs return `CentralityState::Unavailable` with labels and a reason string instead of zero-filled metrics.
- Generic distance handling now uses `DistanceMatrix`, while `SeMatrix` remains as a compatibility alias for older code and artifacts.
- README and math docs now describe the actual MF similarity -> AS distance bridge and clarify that graph centrality is unavailable for pure distance inputs.
- Integration coverage now includes 3D export fidelity, explicit distance-pipeline centrality behavior, single-output vs single-slice-series bridge equivalence, and `OptimalChoice` execution/label preservation.

## Notes on the Medium and Low Fixes

- The MatrixForge GUI now derives defaults from `MfConfig::default()`, exposes `unicode_normalize`, `shared_vocabulary`, and `min_tokens_per_slice` under Advanced options, and explicitly documents that directory ingestion is non-recursive and `.txt`-only.
- AlignSpace JSON output now includes `procrustes` and `etv_dataset`, so the JSON artifact matches the full in-memory result more closely.
- MF and AS now use weighted shortest paths consistently for distance, closeness, and betweenness, with regression tests covering a case where weighted betweenness differs from the old unweighted behavior.
- `MdsDimMode::Visual` is now documented as the legacy public name for the current 2D planar mode, and the GUI label is simplified to `2D`.
- `SimToDistMethod` now lives in `lv-data` and is re-exported from there, removing MF's dependency on an AS-owned bridge type.

## Follow-Up Audit Findings (AS -> LV)

### Confirmed Working Areas

- Core `as-pipeline` adjacency and distance workflows are producing LV-consumable `EtvDataset` outputs, with passing integration coverage in `tests/integration/as_pipeline_e2e.rs` and `tests/integration/mf_pipeline_e2e.rs`.
- 3D coordinate export is preserved through `build_etv_dataset`, and distance-input centrality is now explicit via `CentralityState::Unavailable`.
- MatrixForge single-output and series-output paths both use the AS distance pipeline.

### New Issues Table

| Severity | Difficulty | Area | Description | Evidence | Suggested fix | Expected impact |
|---|---|---|---|---|---|---|
| Critical | Medium | LV runtime | Ego/filtering uses `shape_id` as a label index, so highlighted/visible nodes can map to the wrong labels. | `crates/lv-app/src/main.rs` uses `self.dataset.all_labels.get(inst.shape_id as usize)` while `shape_id` is only a shape enum id from `GpuInstance`. | Carry stable label identity alongside each interpolated instance and stop inferring labels from render-only fields. | Restores correct ego filtering, highlighting, and downstream label-based interactions. |
| High | Medium | LV runtime | Ego picking, visible-set filtering, and ego-edge rendering use `dataset.sheets.first()` and first-sheet positions rather than the active LIS frame. | `crates/lv-app/src/main.rs` uses `self.dataset.sheets.first()` and raw row coordinates in picking, filtering, and edge upload paths. | Drive ego interactions from the active frame and active transition sheet instead of the first sheet. | Makes interaction behavior match the positions the user actually sees during animation. |
| High | Low | AS/LV contract | GUI writes only `EtvDataset` JSON to `as_result.json`, while core AS defines `as_result.json` as full `AsPipelineResult`. | `crates/lv-gui/src/as_panel.rs` serializes `result.etv_dataset`; `crates/as-pipeline/src/output.rs` writes full analytical output to `as_result.json`. | Preserve `as_result.json` for full AS results and write a separate LV-loadable dataset artifact, e.g. `etv_dataset.json`. | Removes contract ambiguity and keeps GUI/runtime behavior aligned with core output APIs. |
| High | Medium | Graph semantics | Directed graph information is not preserved end-to-end: GUI ingestion symmetrizes edges and AS export collapses adjacency pairs with `max`. | `crates/lv-gui/src/as_panel.rs` symmetrizes matrices; `crates/as-pipeline/src/pipeline.rs` exports one edge per pair. | Decide and document whether AS -> LV is undirected-only or implement directed preservation consistently. | Prevents silent loss of graph semantics for directed datasets. |
| Medium | Medium | Temporal fidelity | Independent per-slice normalization after alignment can distort absolute scale comparisons across time. | `crates/as-pipeline/src/normalize.rs` normalizes one coordinate set at a time; `run_pipeline` / `run_distance_pipeline` apply it per slice. | Add an optional global-series normalization mode or document the current tradeoff explicitly. | Improves temporal comparability for scale-sensitive analyses. |
| Medium | Medium | Temporal fidelity | `TimeSeries` Procrustes aligns each slice to the previous aligned slice, which can accumulate drift on long runs. | `align_coordinates` in `crates/as-pipeline/src/pipeline.rs` chains alignment in `TimeSeries` mode. | Add a fixed-anchor time-series mode or document chain-alignment drift tradeoffs. | Reduces long-sequence drift risk. |
| Medium | Low | Label contract | `all_labels` is critical for LIS interpolation but is not validated during normal GUI/runtime loading. | `crates/lv-renderer/src/lis.rs` iterates `dataset.all_labels`; `crates/lv-data/src/validation.rs` does not verify `all_labels` against sheet rows. | Validate or rebuild `all_labels` canonically at load/build time. | Prevents phantom nodes and silent interpolation inconsistencies. |
| Low | Low | API/docs | `LisFrame.slice_index` docs do not match implementation. | `crates/lv-data/src/schema.rs` documents source-sheet index, but `crates/lv-renderer/src/lis.rs` stores a global frame index. | Rename the field or update docs to match actual semantics. | Reduces maintenance confusion. |

### Immediate Implementation Priorities

1. Fix runtime identity and active-frame ego behavior in `lv-app`.
2. Align GUI AS output artifacts with the core `as-pipeline` output contract.
3. Add regression tests covering label-instance identity, active-frame ego behavior, and GUI output artifact layout.

## Medium / Low Follow-Up Implementation Notes

- `EtvDataset::all_labels` is now canonicalized from sheet rows in normal JSON/XLSX load paths, and `validate_dataset` now rejects stale/non-canonical `all_labels` contracts.
- GUI dataset-to-AS ingestion now uses canonical row-label union rebuilding instead of preserving stale extras from incoming `all_labels`.
- `as-pipeline` now supports `NormalizationMode::Global` in addition to the existing per-slice normalization behavior, so callers can preserve relative scale across a whole temporal series.
- `as-pipeline` now supports `ProcrustesMode::TimeSeriesAnchored`, which aligns every later slice to slice 0 instead of chaining slice-to-slice.
- `EtvDataset::estimated_lis_buffer_bytes` now estimates against `all_labels.len()` and includes the single-sheet static-buffer case, matching actual LIS frame construction more closely.
- Renderer/docs/test comments now consistently describe `LisFrame.slice_index` as a global frame index and explain single-sheet LIS behavior more clearly.

## Directed Edge Follow-Up Implementation Notes

- GUI dataset-to-AS ingestion in `crates/lv-gui/src/as_panel.rs` now preserves directed adjacency instead of symmetrizing `from -> to` and `to -> from` automatically.
- `crates/as-pipeline/src/pipeline.rs` now exports one `EdgeRow` per nonzero directed adjacency entry, so reciprocal ties survive as separate LV edges.
- LV ego/runtime behavior now supports `Incoming`, `Outgoing`, and `Both` direction modes, with `Both` kept as the backward-compatible default.
- The main remaining graph-semantics gap is centrality: `crates/as-pipeline/src/centrality.rs` still computes an undirected report from the upper triangle for backward compatibility.
